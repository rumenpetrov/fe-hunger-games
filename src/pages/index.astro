---
import { getEntry } from 'astro:content';
import TextPage from '@src/layouts/TextPage.astro';

const entry = await getEntry('pages', 'home');
const { Content } = await entry.render();
---
<TextPage {...entry.data}>
  <Content />
</TextPage>
